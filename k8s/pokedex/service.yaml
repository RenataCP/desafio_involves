apiVersion: v1
kind: Service
metadata:
  name: pokedex-api
  namespace: pokedex
spec:
  type: NodePort
  selector:
    app: pokedex-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30091
